<div class="container" style="max-width: 500px">
  <div class="page-header">
    <h1>Nuevo registro a artículos</h1>
  </div>

  <form action="<%= to("/articles/registration") %>" method="POST">
    <%= form_group @form, :first_name, "Nombre" %>
    <%= form_group @form, :last_name, "Apellidos" %>
    <%= form_group @form, :email, "Email", input_type: "email" %>
    <%= select_form_group @form, :lead_type, "Tu roll en la boda", @form.lead_type_options %>
    <button type="submit" class="btn btn-default">Enviar</button>
  </form>
</div>
