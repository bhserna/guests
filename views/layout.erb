<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Simple Guest List <%= "| #{@page_title}" if @page_title %></title>

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>
  <body>
    <nav class="navbar navbar-default" style="border: none; border-radius: 0">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="collapsed navbar-toggle" data-toggle="collapse" data-target="#bs-navbar-collapse" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a href="<%= to("/") %>" class="navbar-brand">
            Simple Guest List
          </a>
        </div>
        <div class="collapse navbar-collapse" id="bs-navbar-collapse">
          <ul class="nav navbar-nav">
            <li class="<%= "active" if request.path_info == "/demo" %>">
              <a href="<%= to("/demo") %>">Demo</a>
            </li>
            <li class="<%= "active" if request.path_info == "/lists" %>">
              <a href="<%= to("/lists") %>">Mis listas</a>
            </li>
          </ul>
          <% if user_signed_in? %>
            <form class="navbar-form navbar-right" method="post" action="<%= to("/users/sign_out") %>">
              <button type="submit" class="btn btn-link">Salir</button>
            </form>
          <% else %>
            <a href="<%= to("/users/registration") %>" class="btn btn-primary navbar-btn navbar-right">
              Registrarme
            </a>
            <ul class="nav navbar-nav navbar-right">
              <li><a href="<%= to("/users/login") %>">Iniciar Sesión</a></li>
            </ul>
          <% end %>
        </div>
      </div>
    </nav>

    <%= yield %>
    <%= partial "analytics" %>
    <script src="https://use.fontawesome.com/988af5df22.js"></script>
  </body>
</html>
